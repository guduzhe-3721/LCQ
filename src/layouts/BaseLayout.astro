---
import "../styles/global.css";
import SiteHeader from "../components/SiteHeader.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
const policyUrl = `${import.meta.env.BASE_URL}policy`;
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body>
    <SiteHeader />
    <main class="site-shell">
      <slot />
    </main>
    <footer class="site-footer">
      <div class="site-shell">
        <a href={policyUrl}>网站政策与下架申请</a>
        <span>本站不收集用户数据。</span>
      </div>
    </footer>
  </body>
</html>
